alias edit 'atom-beta'

# the normal rm
alias remove '/usr/local/opt/coreutils/libexec/gnubin/rm'
# the one which uses system trash bin
alias rm 'trash'

alias sbtclean 'remove -rf target/ project/target'
